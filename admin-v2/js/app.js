import{requireAdmin,signOut}from"./auth.js";import{NAV,LABELS}from"./navigation.js";
const nav=document.getElementById("admin-v2-nav"),view=document.getElementById("admin-v2-view"),title=document.getElementById("admin-v2-title");
function renderNav(){nav.innerHTML=NAV.map(g=>'<div class="admin-nav-group">'+g.group+'</div>'+g.items.map(([id,label])=>'<button class="admin-nav-button" data-view="'+id+'">'+label+'</button>').join("")).join("");nav.addEventListener("click",e=>{const b=e.target.closest("[data-view]");if(b)openView(b.dataset.view)})}
function overview(){return '<div class="admin-notice"><strong>Admin V2 isolado.</strong><br>Esta versão ainda não publica nem altera conteúdos do site público.</div><div class="admin-grid admin-section"><article class="admin-card"><small>Agendados hoje</small><strong>—</strong></article><article class="admin-card"><small>Moderação pendente</small><strong>—</strong></article><article class="admin-card"><small>Rascunhos recentes</small><strong>—</strong></article><article class="admin-card"><small>Narrações pendentes</small><strong>—</strong></article></div><section class="admin-section"><h2>Próxima etapa</h2><div class="admin-placeholder">Mapeamento de dados e construção do núcleo editorial unificado: conteúdo, mídia, fontes, SEO, versões, preview, narração e publicação/agendamento.</div></section>'}
function placeholder(id){return '<div class="admin-placeholder"><strong>'+LABELS[id]+'</strong><br><br>Módulo registrado na arquitetura do Admin V2. A integração com banco será habilitada por etapas, após validação das tabelas, RLS e compatibilidade com o painel atual.</div>'}
function openView(id){document.querySelectorAll("[data-view]").forEach(b=>b.classList.toggle("active",b.dataset.view===id));title.textContent=LABELS[id]||"Central Administrativa";view.innerHTML=id==="overview"?overview():placeholder(id);history.replaceState(null,"","#"+id)}
async function boot(){renderNav();try{const session=await requireAdmin();if(!session){view.innerHTML='<div class="admin-notice">Sessão administrativa V2 não encontrada. O login próprio do novo painel será a próxima etapa antes dos testes de acesso.</div>';return}document.getElementById("admin-v2-logout").onclick=async()=>{await signOut();location.reload()};openView(location.hash.slice(1)||"overview")}catch(e){view.innerHTML='<div class="admin-notice">Falha ao iniciar o Admin V2: '+String(e.message||e)+'</div>'}}boot();